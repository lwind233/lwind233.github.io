---
title: 150 逆波兰表达式
tags:
  - 栈与队列
categories:
  - 题解
  - 代码随想录
abbrlink: e77c394a
date: 2026-01-14 21:35:00
---
遇到整数就入栈，遇到运算符就出两个整数，然后将结果入栈

```java
class Solution {
    public int evalRPN(String[] tokens) {
        Stack<Integer> nums = new Stack<>();
        for(int i=0;i<=tokens.length-1;i++){
            if(tokens[i].equals("+") || tokens[i].equals("-") || tokens[i].equals("*") || tokens[i].equals("/")){
                int num2 = nums.pop();
                int num1 = nums.pop();
                if(tokens[i].equals("+")) nums.push(num1+num2);
                else if(tokens[i].equals("-")) nums.push(num1-num2);
                else if(tokens[i].equals("*")) nums.push(num1*num2);
                else if(tokens[i].equals("/")) nums.push(num1/num2);
            }else{
                nums.push(Integer.parseInt(tokens[i]));
            }
        }
        return nums.pop();
    }
}
```
