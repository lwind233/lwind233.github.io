---
title: 234 回文链表
date: 2026-03-03T11:18:00
tags:
  - 链表
  - 双指针
categories:
  - 题解
  - hot100
---

先利用快慢指针找出链表的中点，然后对后半部分链表进行翻转，最后分别从链表的中点和开头开始遍历链表，判断是否一致，一致就是回文链表

```java
class Solution {
    public boolean isPalindrome(ListNode head) {
        if (head == null || head.next == null) return true;

        // 1) 找中点：slow 最终在中点（偶数偏右中点）
        ListNode slow = head, fast = head;
        while (fast != null && fast.next != null) {
            slow = slow.next;
            fast = fast.next.next;
        }

        // 2) 奇数长度：跳过中点
        if (fast != null) { // fast 没到 null 说明长度为奇数
            slow = slow.next;
        }

        // 3) 反转后半段
        ListNode second = reverse(slow);

        // 4) 比较前半段和后半段
        ListNode p1 = head, p2 = second;
        while (p2 != null) { // 后半段更短或相等
            if (p1.val != p2.val) return false;
            p1 = p1.next;
            p2 = p2.next;
        }

        return true;
    }

    private ListNode reverse(ListNode head) {
        ListNode pre = null, cur = head;
        while (cur != null) {
            ListNode nxt = cur.next;
            cur.next = pre;
            pre = cur;
            cur = nxt;
        }
        return pre;
    }
}
```
