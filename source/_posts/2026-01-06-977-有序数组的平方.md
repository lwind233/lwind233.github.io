---
title: 977 有序数组的平方
date: 2026-01-06T11:35:00
tags:
  - 双指针
  - 数组
categories:
  - 题解
  - 代码随想录
---
原数组的特点是，其绝对值（或平方）先递减后递增，也就是最大值在两侧最小值在中间。

用双指针从两侧遍历整个数组，就可以从大到小遍历完整个数组

```java
class Solution {
    public int[] sortedSquares(int[] nums) {
        int left,right;
        left=0;
        right = nums.length-1;


        int[] result = new int[nums.length];
        int count=nums.length-1;
        while(left<=right){
            if(nums[left]*nums[left]>=nums[right]*nums[right]){
                result[count] = nums[left]*nums[left];
                left++;
            }else{
                result[count] = nums[right]*nums[right];
                right--;
            }
            count--;
        }
        return result;
    }
}
```
