---
title: 203 移除链表元素
date: 2026-01-06T16:59:00
tags:
  - 链表
  - 双指针
categories:
  - 代码随想录
---
一个指针指向当前节点的父节点，一个指针指向当前节点

当当前节点的值等于目标值时，往下遍历链表，找到一个值不为目标值的节点，让父节点指向该节点

然后更新父节点指针和当前节点指针的位置

```java
class Solution {
    public ListNode removeElements(ListNode head, int val) {
        ListNode tmpHead = new ListNode();
        tmpHead.next = head;
        ListNode pre = tmpHead;
        ListNode cur = pre.next;


        while(cur!=null){
            while(cur!=null && cur.val==val){
                cur = cur.next;
            }
            pre.next=cur;


            if(cur==null) break;
            else{
                pre=cur;
                cur=pre.next;
            }
            
        }
        return tmpHead.next;
    }
}
```
