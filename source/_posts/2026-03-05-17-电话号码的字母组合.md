---
title: 17 电话号码的字母组合
date: 2026-03-05T20:33:00
tags:
  - 回溯
categories:
  - 题解
  - hot100
---

过于水了

取数字对应的字符列表，遍历其中字符，每次往下回溯将idx加1来取下一位数字，回溯完了删除自身即可

```java
class Solution {
    Map<Character, List<Character>> map = new HashMap<>();
    StringBuilder path = new StringBuilder();
    List<String> result = new ArrayList<>();

    public List<String> letterCombinations(String digits) {
        map.put('2', Arrays.asList('a','b','c'));
        map.put('3', Arrays.asList('d','e','f'));
        map.put('4', Arrays.asList('g','h','i'));
        map.put('5', Arrays.asList('j','k','l'));
        map.put('6', Arrays.asList('m','n','o'));
        map.put('7', Arrays.asList('p','q','r','s'));
        map.put('8', Arrays.asList('v','t','u'));
        map.put('9', Arrays.asList('x','w','y','z'));
        backtrace(digits,0);

        return result;

    }

    void backtrace(String digits, int idx){
        if(path.length()==digits.length()){
            result.add(path.toString());
            return;
        }
        char c = digits.charAt(idx);
        List<Character> tmp = map.get(c);
        for(char cc : tmp){
            path.append(cc);
            backtrace(digits, idx+1);
            path.deleteCharAt(path.length()-1);
        }
    }
}
```
